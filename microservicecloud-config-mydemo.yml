spring:
 profiles:
    active:
      - dev

---
server:
  port: 8231
  
spring:
  profiles: dev
  application:
    name: microservicecloud-config
eureka:
  client:
    service-url:
      defaultZone:  http://eureka-dev.com:7001/eureka/modify-01

---
server:
  port: 8202

spring:
  profiles: test
  application:
    name: microservicecloud-config
eureka:
  client:
    service-url:
      defaultZone:  http://eureka-test.com:7001/eureka/

